import Login from './index';

import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'Compoments/Pages/Login',
  component: Login,
} as Meta<typeof Login>;

export const Primary: StoryObj<typeof Login> = {};
